{
  "scripts": {
    "dev": "ts-node-dev --no-notify --respawn --transpile-only app"
  },
  "dependencies": {
    "@nexus/schema": "^0.18.0",
    "apollo-server-express": "^2.19.0",
    "graphql": "^15.4.0",
    "graphql-scalars": "^1.4.1"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5"
  }
}
